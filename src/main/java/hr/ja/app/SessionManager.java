package hr.ja.app;

public class SessionManager {

//	public UserSession getOrCreate(Session sess) {
//		if (sessions.containsKey(sess.id())) {
//			return sessions.get(sess.id());
//		}
//		UserSession us = new UserSession(sess);
//		sessions.put(us.getId(), us);
//		return us;
//	}
	
}
